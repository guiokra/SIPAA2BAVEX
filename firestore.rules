rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Acesso total público para suportar o modo sem login em qualquer ambiente (Vercel, etc.)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
